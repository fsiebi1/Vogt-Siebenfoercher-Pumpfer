@model CodingLanguages.Models.Message

<h3>@Model.Header</h3>

<p class="paragrath">
    <b>Meldung:</b>
    @Model.MessageText
</p>

@if (Model.Solution != null && Model.Solution.Trim().Length >= 1)
{
    <p class="paragrath">
        <b>Lösung:</b>
        @Model.Solution
    </p>
}